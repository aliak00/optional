{
    // See https://go.microsoft.com/fwlink/?LinkId=733558
    // for the documentation about the tasks.json format
    "version": "2.0.0",
    "tasks": [
        {
            "label": "test",
            "type": "shell",
            "command": "dub test",
            "group": {
                "kind": "test",
                "isDefault": true
            }
        },
        {
            "label": "run",
            "type": "shell",
            "command": "dub run --config=playground",
            "group": {
                "kind": "build",
                "isDefault": true
            }
        }
    ]
}
